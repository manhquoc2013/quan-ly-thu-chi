# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- Chat covers ledger + master CRUD, order pay/deposit/ship updates, rich lookup/reports, navigate to screens.
- Settings mutations (password/API/sync) → `navigate` to `/settings` (not executed in chat).
- Product unit: thú → con; bulk unit update supported.
- Product SKU: sequential numeric (`0001`, `0002`, … via `nextSeqSku`); `generateSkusForProducts`; chat 0e; ProductScreen "Gán SKU thiếu".
- **Inventory:** `Product.stockQty`; stock-in via expense; stock-out when order paid; FIFO report tab.
- **List pagination (hybrid):** CRUD screens use `listQuery` + `usePagedList` + `PaginationBar`. Sticky bulk bar portals to body; same row as AI FAB (shared 48px bottom band).
- **AI priority:** `mergeAiPriority()` appends missing providers from defaults.
- **Mascot:** SVG puppet + RAF ballistic; platforms = card lips / leaf text / inputs / buttons; AuthScreen overlay.

## 2. Recent Changes

- Published GitHub Releases for tags `v1.0.4`…`v1.4.1` (Latest = **v1.4.1**); RELEASE.md now requires `gh release create`.
- Released **v1.4.1** — UI version synced to `package.json` (`APP_VERSION` / `APP_VERSION_LABEL`).
- Version label: single source `package.json` → Vite `__APP_VERSION__` → `APP_VERSION` / `APP_VERSION_LABEL` (Layout, Auth, Onboarding, Settings).
- Released **v1.4.0** (mascot SVG puppet, responsive dashboard/reports, FieldErrorTip, list enter anim).
- Dashboard mobile: last KPI full-width; chart `min-w-0` + no `-mx` overflow; denser Y-axis.
- Customer/product count charts: tooltip & axis use counts (not `đ` currency).
- Report responsive: preset wrap, tabs horizontal scroll, summary cards 1→3 cols, no clip.
- PaginationBar: right inset clears AI FAB (same formula as StickyBulkBar).
- Dashboard KPI: stacked cards; grid 2 / md:6 / xl:5 with span balancing (Đơn chờ full-width on mobile).
- List/table rows: staggered `animate-list-row` enter (grids, product/customer/platform, reports).
- FieldErrorTip shared app-wide (FormField, FormMessage, Auth, dialogs) — auto-hide ~4.2s.
- Onboarding only when authenticated + empty `storeName` (not `!householdId`); household stays in Settings.
- Platforms: text/labels use glyph ink bounds (not full-width flex box) — walk past text → fall.
- Speech bubble: auto-size to text (`w-max`), wrap long lines; open-side tail; pop from head.
- Chute glide: terminal-velocity ease (no per-frame *0.9 jerk); soft open + sway.

## 3. Pending / Next Tasks

- Manual UAT: bubble hugs head + pop anim; hook latch; chute vs hard throw.
- Apply Supabase migrations on remote if not yet: `20260809010000_product_stock.sql`, `20260809020000_user_settings_openrouter_siliconflow.sql`, `20260809030000_revenue_items_zero_price.sql`.
