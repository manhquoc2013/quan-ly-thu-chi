# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- React + Vite PWA; hybrid AI + Unified Intake Pipeline.
- **Text/voice create:** auto-persist (no preview), broad Vietnamese patterns in `textDraftParser`.
- **File OCR/PDF/CSV/XLS:** preview + Confirm via `DataEntryHelper`.
- OCR: Gemini → Tesseract; chat analysis: Gemini → WebLLM.

## 2. Recent Changes

- App bootstrap: `bootstrapAppData()` in Layout hydrates expenses/revenues/customers from IndexedDB on reload (Dashboard/Reports no longer empty until tab switch).
- Text/voice create auto-persist; file OCR/CSV/PDF still need preview confirm.
- Expanded text patterns; 19 unit tests.

## 3. Pending / Next Tasks

- Manual UAT text auto-save vs file preview
- Optional: refresh revenue store after text revenue create; ExpenseDialog edit from OCR preview
- Commit when user requests
