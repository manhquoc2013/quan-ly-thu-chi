# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- Chat covers ledger + master CRUD, order pay/deposit/ship updates, rich lookup/reports, navigate to screens.
- Settings mutations (password/API/sync) → `navigate` to `/settings` (not executed in chat).
- Product unit: thú → con; bulk unit update supported.
- Product SKU: sequential numeric (`0001`, `0002`, … via `nextSeqSku`); `generateSkusForProducts`; chat 0e; ProductScreen "Gán SKU thiếu".
- **Inventory:** `Product.stockQty`; stock-in via expense (`stockProductId` / `stockQtyIn` / `stockApplied`); stock-out when order `paymentStatus=paid` (`Revenue.stockApplied`); restore on cancel/delete/unpaid. Helpers in `stockService.ts`. AI: “nhập …”, TSV bảng nhập hàng (`stockInTableParser`), lookup “tồn kho”.
- **Report Hàng hóa:** FIFO COGS from stock-in lots (`fifoCogsService`); tab nhập tiền mặt + lãi gộp đơn paid + tồn còn lại. Profit tab splits nhập vs chi khác.

## 2. Recent Changes

- Settings: AI toggles beside status badge; **no auto ledger pull on /settings** (was wiping local); hydrate skips empty-cloud overwrite (checks IndexedDB, not just Zustand — auth-before-Layout race).
- Ledger cloud: fire-and-forget dual-write (needs householdId); UI “synced” = settings outbox only. Explicit **Đẩy dữ liệu local lên sổ chung** for full push.
- Cloud `revenues` empty root cause: local `customerId='walk-in'` is not a UUID → RPC FK fail (silent). Fix: map to `WALK_IN_CUSTOMER_ID` + upsert FK parents before revenue.
- Product cloud 409 `products_household_sku_uidx`: upsert on id while SKU owned by another row — retry with `sku=null`. Opening-balance DT không resolve SP / không trừ tồn.
- Report: tab Hàng hóa (nhập + lãi gộp FIFO timeline-ordered); ProfitReport tách Nhập hàng / Chi khác. Spec: `docs/superpowers/specs/2026-08-09-inventory-fifo-report-design.md`.
- Fix: bảng đã thu → DT đã TT (bỏ Tổng khi có chi tiết); chỉ còn dòng Tổng / “không có hàng” → 1 DT đầu kỳ đã TT; hỗ trợ `3.570.000 ₫, ghi nhận vào 31/07`.
- Order TSV / chat: `0đ` / “không cần số tiền” → đơn unpaid **0đ**; nếu pending nhầm **chi phí** thì flip sang revenue; “tạo lại đơn…” bỏ pending cũ. Migration `revenue_items` ≥ 0.
- Inventory / nhập hàng (Approach A): product stock, expense stock-in, order paid stock-out, AI parse + lookup.
- Spec: `docs/superpowers/specs/2026-08-09-inventory-stock-design.md`
- Migration: `supabase/migrations/20260809010000_product_stock.sql`
- Row click → read-only `EntityDetailDialog` (đơn/chi/SP/khách/kênh); không nút thao tác trong dialog.

## 3. Pending / Next Tasks

- Apply Supabase migrations on remote: `20260809010000_product_stock.sql`, `20260809020000_user_settings_openrouter_siliconflow.sql` (fixes user_settings 400).
- Release notes when user asks (inventory + prior SKU/AI ops).
