# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- **App version:** 1.5.0 (release tagged `v1.5.0`).
- **Table horizontal scroll:** `TableHScroll` (`data-table-hscroll`) — `overflow-x: auto` only here; inner `minWidth` (revenue 1100 / expense 800).
- **Page shell:** `main { overflow-x: clip !important }` + `scrollLeft` lock.
- **Buttons:** `[data-slot="button"]` uses `overflow: hidden` + ripple rest `scale(0)` so transforms cannot inflate table `scrollWidth`.
- **Orders:** priority fields + chat "ưu tiên"; parseTaoDonOrder single-item bare `giá` = package total.

## 2. Recent Changes

- Released v1.5.0: priority orders, TableHScroll, ripple/whitespace fix, tạo đơn parsing.

## 3. Pending/Next Tasks

- Apply `20260809170000_revenue_priority.sql` + `20260809180000_upsert_revenue_priority.sql` on Supabase if not yet applied.
- Confirm GitHub Pages deploy for `v1.5.0` succeeded.
