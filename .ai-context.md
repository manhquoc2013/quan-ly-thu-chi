# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- React + Vite PWA; hybrid AI: **Kilo Free → Gemini → WebLLM**.
- Kilo: `kilo-auto/free` via `https://api.kilo.ai/api/gateway` (default on, no key).
- Chat intent + multi-tx sanitize; structured paste uses deterministic parsers.
- Order payment / shipping / Drive sync as before. Pages base `/quan-ly-thu-chi/`.

## 2. Recent Changes

- **Kilo Free:** `kiloService` + `llmCall` cascade; Settings toggle; privacy warning.
- **v1.0.4:** multi-tx + thanh toán CK; WebLLM queue; Vite React dedupe; entity-pick fix.
- **splitMultiTx:** tách cả dấu phẩy khi mỗi vế có tiền; sanitize "{Tên} đã trả …" = doanh thu.
- **Tên SP:** `productQueryFromDescription` không còn cắt `k` trong `kẹp` (bug `\b` ASCII); bỏ “Hoa đã trả…/đi” → còn `kẹp tóc` / `uống nước`.

## 3. Pending / Next Tasks

- UAT lại câu multi-tx — tên hàng phải đúng `kẹp tóc`, `uống nước`, `đổ xăng`.
