# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- React + Vite PWA; hybrid AI + Unified Intake Pipeline.
- **Chat routing:** tin chat → LLM intent (`extractChatIntent` / `extractMultiChatIntents`). Paste bảng/list → parser deterministic.
- **WebLLM:** generate được **queue tuần tự** (không parallel); raw maxTokens 256; không gắn finance context cho create.
- **Sanitize:** `intentSanitize` sửa kind/amount/qty/platform/customer theo đúng câu user.
- **Order payment / shipping / Drive:** như trước. Pages base `/quan-ly-thu-chi/`.

## 2. Recent Changes

- Multi-tx + thanh toán: `splitMultiTx` gắn lại “đã TT / CK” vào đơn bán (không bỏ mất); `paymentStatus`/`paymentMethod` qua sanitize → draft → persist.
- Fix Invalid hook call (Vite dedupe react); entity-pick loop; WebLLM queue.

## 3. Pending / Next Tasks

- UAT lại: `tôi vừa bán cho hoa 3 cái kẹp tóc giá 90k sau đó lại uống nước hết 50k` → 1 doanh thu 90k SL3 + 1 chi phí 50k.
- Nên bật Gemini nếu có — local Qwen vẫn chậm hơn cloud.
