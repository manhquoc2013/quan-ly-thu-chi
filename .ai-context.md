# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- React + Vite PWA; hybrid AI + Unified Intake Pipeline.
- **Order payment / cash-flow:** deposit + paid amounts/dates; doanh thu = cash events; công nợ = final − cọc.
- **Shipping:** `shippingFee` (default 0) + `shippingPayer` default `customer` (cộng final) / `shop` (sync expense).
- **Chat sale rules:** `giá X` = tổng hàng; chỉ `đơn giá` / `giá mỗi cái` / `/cái` = giá từng SP; tách cọc, ship, kênh.
- **Bulk:** expense line-list; order TSV (`Ship=` → shippingFee).

## 2. Recent Changes

- WebLLM: `context_window_size` 8192; `generate({ mode:'raw'|'chat' })` tránh chồng system + cắt ngữ cảnh.
- Chat parser cọc/ship/giá; shipping fee; OrderDialog ~960px.

## 3. Pending / Next Tasks

- Manual UAT: `Như mua 3 kẹp tóc giá 90k, đã cọc 30k ở Zalo, khách chịu phí ship 11k`
- Commit when user requests
