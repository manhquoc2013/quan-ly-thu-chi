# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- **App version:** 1.7.0.
- **Orders search:** shared `revenueFilters.ts` (orderCode / notes / buyer name / walk-in); cloud prefers RPC `search_revenues_page` with 2-query fallback.
- **Lists:** local hybrid paging prefers in-memory store over IDB; Revenue/Expense search debounce once (input debounce + `usePagedList debounceMs=0`).
- **Realtime:** cloud active → `hydrateStoresFromCloud` on remote change.
- **Dashboard:** action-first via `buildDashboardSnapshot` — month KPIs, one queue, products, then chart/recent.
- **Report:** Overview default; groups Tài chính / Vận hành; `buildReportSnapshot`.
- **Auth brand:** ink navy + soft sand; Syne/Figtree; Auth + Onboarding `.auth-surface`.
- **Print:** OrderBill A6 (`window.print` + `@page A6`); entry from grid + detail.
- **LLM:** local-first slot-fill + 30m TTL; skip bulk without newline/table; short MERGE_PROMPT; sanitize once; no duplicate chat cascade.

## 2. Recent Changes

- Implemented full optimize plan waves W1 → WD → WR → WB → WP → W2 → W3 (RPC migration + usage maps + single RevenueGrid layout).
- Migration: `supabase/migrations/20260812110000_search_revenues_page.sql` (apply on each env).

## 3. Pending/Next Tasks

- Apply `search_revenues_page` migration on Supabase environments.
- Apply priority migrations if still pending on any env.
