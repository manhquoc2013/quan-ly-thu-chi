# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- React + Vite PWA; shared ledger + Supabase Auth + settings sync.
- Chat data ops: ledger + master (product/customer/platform) via extended ChatIntent → chatTools.
- Bulk paste policy A: `isClearBulkPaste` (explicit header) → local persist; else LLM bulk (`expense|revenue|product`) or ask for header.

## 2. Recent Changes

- Chat reply render: Markdown hard line-breaks + structured emoji summaries (bulk SP/chi/đơn list từng dòng).
- Spec: `docs/superpowers/specs/2026-08-08-llm-app-data-ops-design.md`
- Clear-gate, LLM bulk product, master CRUD intents.

## 3. Pending / Next Tasks

- Manual UAT: paste catalog, unlabeled list + LLM, đổi giá SP, xóa khách (confirm).
- Redeploy / version bump when ready for Pages.
