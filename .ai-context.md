# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- React + Vite PWA; branch `feat/supabase-shared-ledger`.
- Auth: **Supabase Auth only** (local IndexedDB login removed from gate).
- Settings/profile: `profiles` + `user_settings` in Postgres; local outbox + flush when online; pull on login.
- Ledger: household dual-write/hydrate (existing); offline settings outbox Phase 1.

## 2. Recent Changes

- Implemented auth+settings sync plan: migration `20260808183000_user_profiles_settings.sql`, syncOutbox/syncEngine/sessionBootstrap, AuthScreen/AuthGuard/AuthProvider wired to Supabase.

## 3. Pending / Next Tasks

- Run new SQL migration in Supabase SQL Editor.
- Manual UAT: register → set keys → reload → offline edit → sync.
- Fill `VITE_EMAILJS_PUBLIC_KEY`; deploy kilo-proxy.
- Phase 2: full ledger outbox.
