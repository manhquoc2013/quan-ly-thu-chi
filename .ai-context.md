# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- **Table horizontal scroll:** `TableHScroll` (`data-table-hscroll`) — `overflow-x: auto` only here; inner `minWidth` (revenue 1100 / expense 800). Rows use inline `gridTemplateColumns`.
- **Page shell:** `main { overflow-x: clip !important }` + `scrollLeft` lock — so `overflow-y: auto` never opens page-level X scroll (that was the layout break).
- Chat / inventory / priority / mascot unchanged from prior context.

## 2. Recent Changes

- Restored intentional table X-scroll via `TableHScroll`; kept main X-clip so layout cannot shift under sidebar.

## 3. Pending / Next Tasks

- Hard refresh: wide viewport = full columns; narrow = scrollbar under table only; Mã đơn visible at scrollLeft 0.
- Apply `20260809180000_upsert_revenue_priority.sql` on Supabase.
