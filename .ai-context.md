# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- **Table horizontal scroll:** `TableHScroll` (`data-table-hscroll`) — `overflow-x: auto` only here; inner `minWidth` (revenue 1100 / expense 800). Rows use inline `gridTemplateColumns`.
- **Page shell:** `main { overflow-x: clip !important }` + `scrollLeft` lock — so `overflow-y: auto` never opens page-level X scroll.
- **Buttons:** `[data-slot="button"]` uses `overflow: hidden` + ripple `::after` rest `scale(0)` (not `clip-path` / rest `scale(10)`) so transforms cannot inflate table `scrollWidth`.
- Chat / inventory / priority / mascot unchanged from prior context.

## 2. Recent Changes

- Fixed table trailing whitespace / phantom X-scroll: button ripple in `src/index.css` was expanding `TableHScroll` scrollWidth (root cause since 50604c8).

## 3. Pending/Next Tasks

- Hard refresh Doanh thu / Chi phí: table fills card; no empty gap after last column; no phantom horizontal scroll.
- Apply `20260809180000_upsert_revenue_priority.sql` on Supabase.
