# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- **App version:** 2.0.0.
- **Auth typography:** Be Vietnam Pro (display) + Figtree (body).
- **Mascot copy:** `src/services/mascotLines.ts`.
- **Search / hydrate / print / Dashboard / Report / LLM:** as 2.0.0 release notes.

## 2. Recent Changes

- Release v2.0.0: package.json, CHANGELOG, RELEASE.md.

## 3. Pending/Next Tasks

- Apply search RPC migrations on Supabase (`…110000` + `…120000`).
- After merge/push on main: tag `v2.0.0` + `gh release create` (Pages deploy).
