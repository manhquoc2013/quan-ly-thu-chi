# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- Chat covers ledger + master CRUD, order pay/deposit/ship updates, rich lookup/reports, navigate to screens.
- Settings mutations (password/API/sync) → `navigate` to `/settings` (not executed in chat).
- Product unit: thú → con; bulk unit update supported.
- Product SKU: sequential numeric (`0001`, `0002`, … via `nextSeqSku`); `generateSkusForProducts`; chat 0e; ProductScreen "Gán SKU thiếu".
- **Inventory:** `Product.stockQty`; stock-in via expense; stock-out when order paid; FIFO report tab.
- **List pagination (hybrid):** CRUD screens use `listQuery` + `usePagedList` + `PaginationBar`. Sticky bulk bar portals to body; same row as AI FAB (shared 48px bottom band).
- **AI priority:** `mergeAiPriority()` appends missing providers from defaults.
- **Mascot:** SVG puppet (`CatBody`) with pixel-inspired actions; RAF position; activity levels; pointer toss/tap; grapple hook SVG; scroll-fall.

## 2. Recent Changes

- Auth login: no WebLLM chip / no interactive mascot. WebLLM preload only in Layout when `enableWebLLM`.
- Mascot: ledge platforms; forward-only; rope up only; land FX.

## 3. Pending / Next Tasks

- Manual UAT: loading + login backgrounds, mascot readable on photo, form contrast.
- Apply Supabase migrations on remote if not yet: `20260809010000_product_stock.sql`, `20260809020000_user_settings_openrouter_siliconflow.sql`, `20260809030000_revenue_items_zero_price.sql`.
