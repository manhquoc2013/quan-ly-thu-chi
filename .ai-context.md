# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- React + Vite PWA; hybrid AI + Unified Intake Pipeline.
- **Text/voice create:** auto-persist (no preview), broad Vietnamese patterns in `textDraftParser`.
- **File OCR/PDF/CSV/XLS:** preview + Confirm via `DataEntryHelper`.
- OCR: Gemini → Tesseract; chat analysis: Gemini → WebLLM.
- Charts use raw VND + `src/utils/chartFormat.ts` (axis `25k`/`1,5tr`, tooltip `formatCurrency`).

## 2. Recent Changes

- Chat: textarea multi-line (Shift+Enter xuống dòng); parse `bán cho {khách} {SL} {SP} giá {đơn giá}` đúng (qty × unit).
- DatePicker: calendar icon right-aligned (Popover + Calendar).
- All selects → searchable `Dropdown` (cmdk) with options from label maps (`optionsFromLabels`).
- Expense UI: removed payment status filter/column/actions (chi phí không dùng trạng thái thanh toán).
- Order detail (`OrderRowCard`): clean meta grid (customer name, amount, status) — no UUID clutter.
- Order edit (`OrderDialog`): sync form on open/edit; prefill items/customer; tighter layout.
- RevenueGrid: Sửa/Xóa `stopPropagation` + confirm delete; show customer name not UUID.
- Dashboard + Profit/Expense/Revenue reports: chart values in VND; readable tooltips/axis.
- App bootstrap still hydrates IndexedDB on reload via `bootstrapAppData()`.

## 3. Pending / Next Tasks

- Manual UAT: order detail/edit + chart tooltips after AI create
- Commit when user requests
