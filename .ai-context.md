# AI Context — quan-ly-thu-chi

## 1. Current Architectural State

- React + Vite PWA; hybrid AI: **Kilo Free → Gemini → WebLLM**.
- Kilo: `kilo-auto/free` via Vite proxy `/api/kilo` (dev) — Gateway chặn CORS browser.
- Chat intent + multi-tx split/sanitize; local create fallback khi LLM trả văn xuôi.
- Pages base `/quan-ly-thu-chi/`. **v1.0.5**

## 2. Recent Changes

- **v1.0.5:** Kilo Free + CORS proxy; multi-tx comma split; local create fallback; badge Kilo/Gemini; fix tên SP (`kẹp` không bị cắt `k`); typo `nết`→`hết`.
- **v1.0.4:** multi-tx + thanh toán CK; WebLLM queue; Vite React dedupe.

## 3. Pending / Next Tasks

- Prod Pages: Kilo cần reverse proxy (`VITE_KILO_GATEWAY_BASE`) hoặc fallback Gemini/WebLLM.
- UAT Pages sau deploy tag `v1.0.5`.
