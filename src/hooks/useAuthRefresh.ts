/**
 * useAuthRefresh — stub (auto-refresh handled by AuthProvider).
 *
 * Kept for compatibility; the actual refresh logic lives in AuthProvider.
 */

export function useAuthRefresh(): void {
  // No-op: refresh is managed by AuthProvider directly.
}
