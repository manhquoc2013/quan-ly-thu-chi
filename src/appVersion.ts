/** App semver from `package.json` (injected by Vite `define`). */
export const APP_VERSION = __APP_VERSION__;

/** Display label, e.g. `v1.4.0`. */
export const APP_VERSION_LABEL = `v${APP_VERSION}`;
