# BA-Lite Brief — TRI-1785658389672-8b57

*C2 reduced pipeline — BA-lite replaces full BA stage per ADR-0038 §7.*

## WHAT

Replace all system prompts across 4 service files with the "Mèo Lucky" store assistant persona and update the AI action parser to handle Vietnamese action types. Add a floating mascot overlay (Lucky the cat) with CSS animations triggered by transactions.

### 1. System Prompt Replacement

Replace the full system prompt in each file with the "Mèo Lucky" prompt from the triage record:
- **Personality**: Lucky the cat — fast, witty cashier assistant who loves counting money 🐱. Cheers on sales, gently reminds on expenses.
- **Store entities**: transaction types (thu/chi), sales channels (Shopee, TikTok, Facebook, Khách ghé tiệm, Khách quen), expense categories (Nhập hàng, Tiền nhà/Điện nước, Bao bì/Đóng gói, Chi khác).
- **Output format**: JSON with `action` (BAN_HANG/CHI_PHI/XEM_BAO_CAO/TAN_GAU), structured `data` (khach_hang, kenh_ban, don_hang[], chi_tiet_chi), `mascot_say`, `mascot_emotion`.

**Files:**
- `src/services/webLLM.ts` → replace `CHAT_SYSTEM`
- `src/services/geminiService.ts` → replace `SYSTEM_INSTRUCTION`
- `src/services/llmIntentExtractor.ts` → replace `EXTRACT_PROMPT` header/schema, KEEP extraction rules
- `src/services/llmBulkDraftExtractor.ts` → replace `BULK_PROMPT`

### 2. parseAiAction() in aiRouter.ts

Map Vietnamese actions: BAN_HANG→create_revenue, CHI_PHI→create_expense, XEM_BAO_CAO→lookup, TAN_GAU→chat.
Handle new JSON structure with Vietnamese keys.
Trigger mascot: `useMascotStore.getState().speak(mascot_say, mascot_emotion)`.

### 3. Mascot Overlay

- `src/store/mascotStore.ts` — add `thinking` emotion
- `src/ui/components/MascotOverlay.tsx` — NEW: fixed bottom-right z-50, slide-up+bounce entrance, emotion emoji + speech bubble, auto-hide 4s
- `src/ui/Layout.tsx` — Add `<MascotOverlay />` before closing tag

## ENTRY POINT

Chat input → AI generate + MascotOverlay trong Layout

## ORACLE

1. `bun run typecheck` exits zero
2. All prompts replaced with "Mèo Lucky" content
3. parseAiAction() maps Vietnamese actions correctly
4. MascotOverlay renders with correct emoji/message
5. `bun run build` exits zero
